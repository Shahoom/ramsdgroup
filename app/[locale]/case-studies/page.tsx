import type { Locale } from "@/i18n/routing";
import { PageIntro } from "@/components/content/PageIntro";
import { Container } from "@/components/ui/Container";
export default async function CaseStudiesPage({ params }: { params: Promise<{ locale: Locale }> }) { const { locale } = await params; const ar = locale === "ar"; return <><PageIntro eyebrow={ar ? "دراسات الحالة" : "CASE STUDIES"} title={ar ? "خبرة موثقة، بلا كشف لمعلومات العملاء" : "Documented experience without exposing client information"} description={ar ? "سننشر دراسات مجهّلة فقط بعد موافقة العميل والتحقق من كل نتيجة أو رقم." : "Only anonymized, client-approved case studies with verified outcomes will be published."} /><section className="py-20"><Container><div className="rounded-3xl border border-dashed bg-surface p-10 text-center"><h2 className="text-2xl font-bold">{ar ? "قيد المراجعة والتحقق" : "Under review and verification"}</h2><p className="mx-auto mt-3 max-w-2xl text-muted">{ar ? "لم ننشر أمثلة غير موثقة أو نتائج تسويقية مصطنعة. لوحة الإدارة جاهزة لإضافة الحالات المعتمدة لاحقًا." : "We have not published unverified examples or fabricated outcomes. The CMS is ready for approved cases when available."}</p></div></Container></section></>; }
