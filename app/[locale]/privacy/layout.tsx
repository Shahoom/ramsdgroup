import type { Metadata } from "next"; import type { Locale } from "@/i18n/routing"; import { buildMetadata } from "@/lib/seo";
export async function generateMetadata({ params }: { params: Promise<{ locale: Locale }> }): Promise<Metadata> { const { locale } = await params; return buildMetadata({ locale, path: "/privacy", title: locale === "ar" ? "سياسة الخصوصية | رام" : "Privacy Policy | RAM", description: locale === "ar" ? "سياسة معالجة بيانات طلبات التواصل في موقع رام." : "How RAM handles contact-request data submitted through this website." }); }
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
