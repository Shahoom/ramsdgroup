import type { ContentBlock } from "@/lib/content/schema";

export function ContentBlocks({ blocks }: { blocks: ContentBlock[] }) { return <div className="space-y-12">{blocks.map((block, index) => { if (block.type === "rich_text") return <section key={index}>{block.heading && <h2 className="text-3xl font-bold">{block.heading}</h2>}<div className="mt-5 space-y-5 text-lg leading-8 text-muted">{block.paragraphs.map((p) => <p key={p}>{p}</p>)}</div></section>; if (block.type === "steps") return <section key={index}><h2 className="text-3xl font-bold">{block.heading}</h2><ol className="mt-7 grid gap-4">{block.items.map((item, i) => <li key={item.title} className="grid grid-cols-[3rem_1fr] gap-4 rounded-2xl border bg-surface p-5"><span className="text-2xl font-bold text-gold-500">{String(i + 1).padStart(2, "0")}</span><div><h3 className="font-bold">{item.title}</h3><p className="mt-1 text-muted">{item.text}</p></div></li>)}</ol></section>; if (block.type === "callout") return <aside key={index} className="rounded-[1.75rem] border border-gold-400/30 bg-gold-400/10 p-7"><h2 className="text-xl font-bold">{block.heading}</h2><p className="mt-3 text-muted">{block.text}</p></aside>; return <blockquote key={index} className="border-s-4 border-gold-400 ps-6 text-2xl">{block.quote}</blockquote>; })}</div>; }
