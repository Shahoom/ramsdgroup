import { Container } from "@/components/ui/Container";

export function LegalArticle({ updated, sections }: { updated: string; sections: { title: string; paragraphs: string[]; items?: string[] }[] }) {
  return <section className="py-20"><Container><div className="grid gap-12 lg:grid-cols-[240px_minmax(0,760px)] lg:justify-center"><aside><div className="sticky top-28 rounded-2xl border bg-surface-2/45 p-5 text-sm text-muted"><p className="font-bold text-foreground">{updated}</p><ol className="mt-4 space-y-2">{sections.map((section, index) => <li key={section.title}><a href={`#legal-${index + 1}`} className="hover:text-gold-700">{section.title}</a></li>)}</ol></div></aside><article className="space-y-12">{sections.map((section, index) => <section key={section.title} id={`legal-${index + 1}`} className="scroll-mt-28"><p className="text-sm font-bold text-gold-700">{String(index + 1).padStart(2, "0")}</p><h2 className="mt-2 text-3xl font-bold">{section.title}</h2><div className="mt-5 space-y-4 text-lg leading-8 text-muted">{section.paragraphs.map((paragraph) => <p key={paragraph}>{paragraph}</p>)}</div>{section.items ? <ul className="mt-6 grid gap-3 sm:grid-cols-2">{section.items.map((item) => <li key={item} className="rounded-xl border bg-surface p-4 leading-7 text-muted">{item}</li>)}</ul> : null}</section>)}</article></div></Container></section>;
}
