import { ArrowUpLeft } from "lucide-react";
import { Link } from "@/i18n/navigation";
import type { LocalizedEntry } from "@/lib/content/schema";

export function EntryCard({ entry }: { entry: LocalizedEntry }) { const base = entry.type === "article" ? "/insights" : entry.type === "service" ? "/services" : "/industries"; return <Link href={`${base}/${entry.slug}`} className="group relative flex min-h-64 flex-col overflow-hidden rounded-[1.75rem] border border-border bg-surface p-7 shadow-elevated transition duration-300 hover:-translate-y-1 hover:border-gold-400/50"><span className="text-xs font-bold uppercase tracking-[.18em] text-gold-600">{entry.eyebrow}</span><h3 className="mt-5 text-2xl font-bold group-hover:text-navy-600 dark:group-hover:text-gold-300">{entry.title}</h3><p className="mt-4 text-muted">{entry.excerpt}</p><span className="mt-auto flex items-center gap-2 pt-7 text-sm font-bold text-gold-700 dark:text-gold-300">{entry.locale === "ar" ? "اكتشف التفاصيل" : "Explore details"}<ArrowUpLeft className="size-4 transition group-hover:-translate-x-1 group-hover:-translate-y-1" /></span><span className="absolute -bottom-16 -end-16 size-40 rounded-full bg-gold-400/8 transition group-hover:scale-125" /></Link>; }
